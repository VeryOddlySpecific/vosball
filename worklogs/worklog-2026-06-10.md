# Worklog — 2026-06-10

## Summary
Wide-ranging session on branch `feat/league-park-factors`: header chips gained
a per-league sim date; depth charts went stats-first with true vs-LHP / vs-RHP
platoon views (position depth **and** lineups); Trade Targets gained
side-specific **Platoon Targets** sections on top of the untouched overall
list; the new `/lgdata` endpoint was wired in (game-world structure snapshot +
standings on the League Hub, grouped by level with one league per column).
Closed with a security audit that found two real StatsPlus tokens (ndl, bwb)
committed in the **public** repo — scrubbed the working tree and added a guard
test; **tokens still need rotating**.

## Files Changed
**`core/preflight.py` — sim date as data:**
- `PreflightResult` gained `current_date: Optional[str] = None`, populated on
  the two paths where `/exports` returns it (previously only embedded in the
  human-readable `reason` string).

**`webapp/status.py` — league date under header chips:**
- `export_status` passes `date` through the cached payload; `render_band`
  renders a `.league-date` div (LCARS font, accent color) under each chip.
  Same cache as the status itself — refreshes on ⟳; `—` when unknown.

**`core/depth_chart.py` — split-blended scores on player records:**
- `build_player_record` now also stores `composite_vs_l/vs_r` and
  `pos_scores_blended_vs_l/vs_r`, using the identical renormalized
  ratings/stats blend as the overall composite (split sample weight was
  already computed, previously discarded). Raw-0 positions stay 0 — a hot
  bat vs one hand can't create position viability. Additive keys; CLI
  consumers unaffected.

**`webapp/depth.py` — stats-first + platoon depth charts:**
- "Blend in-season stats" toggle now defaults **on** (untick = old offline
  ratings-only behavior).
- `build_depth` re-runs `assign_positions` + `build_position_depth_table`
  per split over pools with split-blended scores swapped in — starters CAN
  differ per split (that's the platoon). Position depth renders as
  **Overall / vs RHP / vs LHP** tabs when stats are on.
- Lineups now derive from each split chart's own starters
  (`build_lineup(split starters, split)`) with per-split `pos_by_pid_r/_l`
  maps — the vs-LHP lineup is exactly the vs-LHP chart's starting nine.

**`core/trade_targets.py` — platoon targets core:**
- New `PLATOON_EDGE_MIN = 2.0` and `build_platoon_targets(split, org_hitters,
  candidates)`: re-grades hitter-position needs with org composites swapped
  to the split blend, then keeps candidates that (a) have split stats,
  (b) beat their own overall composite by ≥ the edge, (c) fill a non-Set
  need vs that hand. Fit reuses `compute_fit_score` so rankings share the
  main list's scale. Overall pipeline untouched (user's explicit ask).

**`webapp/trade_targets_page.py` — platoon sections:**
- `build_targets_context` computes platoon data for both splits (stats runs
  only); new "⚖️ Platoon targets — side-specific holes" section below the
  category breakdown: vs LHP / vs RHP lists with a per-side needs expander
  and a table showing Split Comp / Overall / Edge. Min-composite slider
  applies to the split composite.

**`core/lgdata.py` — NEW: /lgdata fetch + structure/standings helpers:**
- `fetch_lgdata` (calendar-day disk cache, token off the cache key,
  validate-before-cache, fail-open), `evict_cache` (forced mid-day re-pull),
  `write/load_structure_snapshot` → `config/structure-{league}.json`
  (structure retained offline; standings excluded as point-in-time),
  `division_standings` (joins teams onto records; exposes
  `sub_league_id`/`subleague`/`division`), `leagues_by_level` (level
  ascending, league_id order within level — IL before PCL),
  `load_level_labels` (id_maps.json `league_level`, inverted),
  `max_games_played`, `league_options`.

**`webapp/league.py` — standings on the League Hub:**
- New Standings section between checklist and modules. Level picker
  ("AAA — IL · PCL"; labels from id_maps so they match the rest of the app).
  Rendering is block-based: a block (game-world league, or subleague when the
  level is one league) never splits or interleaves across columns — AL | NL,
  IL | PCL, EL | SL | TL; >3 blocks (six A-ball leagues) round-robin whole
  blocks across 2 columns. Per-column "Season not started (0 games)" caption
  for mixed-season levels. Streak rendered W3/L2; magic# -1 → ✓ (clinched),
  ≥1000 → —. ⟳ evicts both session and disk cache.
- `config/structure-wwoba.json` written (new, untracked — should be
  committed; same pattern as teams-wwoba.json).

**Security scrub (public repo!):**
- `core/league_registry.py` — real ndl token in the `validate_token`
  docstring example replaced with fake `019e0000-…`.
- `tests/test_league_registry.py` — real ndl token (3×) → fake `019e1111-…`;
  real bwb token → fake `019e2222-…`; dependent `startswith` asserts updated.
- `webapp/tests/test_webapp.py` — real ndl token → fake; `startswith` updated.
- `tests/test_no_secrets.py` — NEW guard: harvests UUID-shaped values from
  the local gitignored secret stores and fails if any appears in any
  git-tracked file (prints only 8-char prefixes). Skips cleanly on fresh
  clones / no git.

## Deviations from Plan
- Trade Targets platoon work was originally sketched as "grade needs per
  split and let candidates match side-specific holes" — the user adjusted to
  **keep the overall table intact** and add separate LHP/RHP Platoon Target
  sections; implemented that way (specialists-only via the edge gate).
- Standings layout took three iterations driven by screenshots/feedback:
  flat 2-col round-robin → one column per subleague → level-based picker
  with one league per column. The level grouping is purely by `level` value,
  so KBO's rookie affiliate (KFL) groups under R with the MLB complex
  leagues — known, accepted simplification (`parent_league_id` exists if we
  ever want per-organization trees).
- The token leak was found mid-session from a direct user question, not
  planned work; fixing it (replacements + guard test) was added to scope.

## Test / Validation Results
- Synthetic smoke tests (written, run, deleted): split depth assignment
  platoons correctly (identical overall composites, different per-split 1B
  starters); `build_platoon_targets` surfaces the lefty-masher vs a
  Critical-vs-LHP hole and returns nothing vs RHP.
- Live endpoint verification: `/lgdata` fetch (5 sections, 230 teams),
  disk-cache round-trip identical, snapshot written, FSL/MLB division tables
  and the full level grouping (ML/AAA/AA/A/R/INT) all correct.
- `py tests/test_league_registry.py` — all pass after token replacements.
- `py webapp/tests/test_webapp.py` — all 33 checks pass.
- `py tests/test_no_secrets.py` — OK: 8 secret values vs 193 tracked files,
  no leaks (verified it would have caught the original paste).
- Compile checks on every touched module.

## Resume Steps
1. **ROTATE the ndl and bwb StatsPlus tokens** — they're in pushed public
   history (since commit `c886da0`) and stay burned until rotated. Update
   `config/statsplus_tokens.json` afterward; the guard test then protects
   the new values. (History scrub via `git filter-repo` is optional hygiene
   after rotation.)
2. Commit this session's work (everything is uncommitted on
   `feat/league-park-factors`), including new files `core/lgdata.py`,
   `tests/test_no_secrets.py`, `config/structure-wwoba.json`.
3. Restart the running Streamlit app (or hit each ⟳) — session caches hold
   pre-change result shapes for the status band, depth, trade targets, and
   standings.
4. Open ideas, in rough priority: lgdata Phase 3 (hook into
   `league_provision.py` alongside `/teams`; division context on trade
   screens; consider lgdata as the authoritative team-id source — the old
   `teams-wwoba.json` has 262 entries vs lgdata's current 230); CLI
   `trade_targets.py` report doesn't render the platoon sections yet;
   `run_depth_chart_all.py` / CLI depth report could adopt the split depth
   tables.
