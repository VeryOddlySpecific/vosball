# Worklog — 2026-06-03

## Summary
Published the previously-local-only VOSBall repo to GitHub
(github.com/VeryOddlySpecific/vosball), then did a full root-level reorganization:
audited all 50 root `.py` files by import graph, moved the standalone CLI tools
into `tools/` and the web-app-consumed modules into `core/`, relocated the docs
into `docs/`/`docs/archive/`, archived the dead `vos_v2` engine, rewrote the
README + 14 keeper guides to the new `vosball`/UI-first format, and added a
pre-push hook that auto-untracks any `.gitignore`'d files. 14 commits, all pushed.

## Files Changed
**Publishing the repo (commits `ce5c733`…`36291ec`):**
- Committed three prior sessions of uncommitted work (Free Agents, Trade Targets,
  Farm Value + the 06-01 UI siloing) as feature-grouped commits; the
  cross-cutting shell files (`webapp/app.py`, `webapp/league.py`,
  `webapp/tests/test_webapp.py`) went into one "wire pages into the app shell"
  commit; `config/league_settings.json` + tickets + `worklogs/` separately.
- Renamed branch `master` → `main`; added remote `origin` (canonical-cased
  `VeryOddlySpecific/vosball`); pushed. Security-scanned first — `.gitignore`
  correctly excludes `statsplus_tokens.json`/cookies/league output; nothing
  secret tracked.

**`tools/` move — 34 standalone CLI tools (`aa8ee79`, `97f471b`):**
- `git mv` of 34 root tools into `tools/`; each got an anchor fix
  (`Path(__file__)...parent` → `.parent.parent`, restoring repo-root for
  config/data/output) and a `sys.path` bootstrap so bare sibling + `vosball`/`lib`
  imports still resolve when run as `py tools/X.py`.
- `tools/run_waiver_wire_all.py` — `WAIVER_SCRIPT` retargeted to
  `tools/waiver_wire.py`. README commands + layout tree updated.

**`core/` move — 15 web-app-consumed modules (`6478035`):**
- `git mv` of the 15 modules the UI imports in-process (depth_chart, stats,
  trade_targets, farm_value, contract, fetch_player_data, …) into `core/`,
  leaving only `run_vos.py` at root. Same anchor fix + a shared bootstrap that
  adds **both** repo-root and `core/` to `sys.path`.
- `tools/` bootstraps upgraded to the same shared block (so tools can import the
  relocated core modules).
- `webapp/app.py` + the 5 core test suites + `webapp/tests/test_webapp.py` add
  `ROOT/core` to `sys.path`; batch-runner target paths retargeted to
  `core/depth_chart.py`, `core/trade_targets.py`, `core/fetch_player_data.py`.

**Docs reorg (`a5bf3fd`):**
- Moved every root `.md` except `README.md` into `docs/` (15 keepers) and
  `docs/archive/` (7 superseded/ephemeral: RESUME, SESSION_NOTES, forum_vospectus,
  instructions, USER_GUIDE, DRAFT_GRADES_PHASE4, REFACTOR_LOG). Nothing deleted.
- Repathed all in-doc markdown links to new code/doc locations; fixed the
  `webapp/README.md` → `LOGIC_UPDATE_PROCESS.md` link; added `docs/` to README tree.

**Engine archive + doc rewrite (`41ab47e`):**
- `tools/vos_v2.py` → `tools/archive/vos_v2.py` (anchor depth fixed for the extra
  nesting); `docs/README_VOS_V2.md` → `docs/archive/`.
- Rewrote `README.md` UI-first (retitled `ratings` → **VOSBall**): leads with the
  Streamlit app + page table; `run_vos.py`/`vosball` + `core/`/`tools/` underneath.
- Updated the 14 keeper docs (via 5 parallel subagents, then verified):
  `vos_v2.py` → `run_vos.py` (VOS v10/`vosball`), commands repathed to
  `core/`/`tools/`, PowerShell-style, app-first framing where a page exists,
  stale sandbox/absolute paths stripped. `docs/DRAFT_WORKFLOW.md` external
  research links de-linked (never in this repo).

**Pre-push ignored-file guard (`19f610b`, `1ea7e05`):**
- `.githooks/pre-push` — auto-untracks any tracked file matching `.gitignore`,
  commits the removal, blocks the push (re-push sends it). `core.hooksPath` set to
  `.githooks` for this clone.
- `.gitattributes` — forces LF on `.githooks/**` + `*.sh` so `/bin/sh` doesn't
  choke on CRLF.

## Deviations from Plan
- **Phase 2 reshaped by user.** The original plan was to leave the 15 web-app
  modules at root as an "engine + app-core" layer (or move them into `tools/`).
  The user instead asked for a dedicated `core/` directory, which is cleaner —
  root now holds only `run_vos.py`.
- **Docs: archive, not delete.** Triage flagged 6 stale docs as delete candidates;
  the user chose to *move* everything-but-README into `docs/`, with the stale ones
  under `docs/archive/` — non-destructive.
- **"Force refresh to remove ignored files" → corrected.** The user asked to set
  up a force-push that strips ignored items already on the repo. Clarified that
  (a) a force push doesn't remove ignored files (they aren't tracked), and (b) the
  repo had **zero** tracked-ignored files anyway. Delivered the requested intent
  as a pre-push auto-untrack hook (their chosen behavior) instead.
- **Subagents corrected real doc inaccuracies.** While rewriting guides, the doc
  subagents read source and fixed wrong flags in the old docs (engine flag is
  `--draft`, not `--draft-mode`; several `farm_value`/`budget_audit` options were
  wrong) — also fixed the same `--draft` slip in the new README.

## Test / Validation Results
- Run at **every** structural phase (tools move, core move, docs move, vos_v2
  archive): `tests/test_golden.py` → **4/4** (engine byte-identical);
  `webapp/tests/test_webapp.py` → **24/24**; the 5 core suites
  (`test_depth_provenance`, `test_fa_fit`, `test_fa_freshness`,
  `test_farm_value_core`, `test_trade_targets_core`) → all pass.
- Import harnesses: all **34** `tools/` + **15** `core/` modules import in
  isolation (only their own dir on `sys.path`); the moved `tools/archive/vos_v2.py`
  imports and anchors `SCRIPT_DIR` to repo-root.
- Real runs: `core/depth_chart.py` and `tools/org_strength_report.py` write output
  to repo-root `ndl/…`, **nothing stray** under `core/`/`tools/`.
- Docs: all keeper-doc relative links resolve (0 broken); the only dangling links
  are external `OOTP Study 27/` refs in archived `SESSION_NOTES` (intentional).
- Pre-push hook tested live: simulated a committed ignored file → hook untracked
  it, kept it on disk, made a cleanup commit, blocked the push (exit 1); test
  commits reset away cleanly.

## Resume Steps
- **Repo is on GitHub** at github.com/VeryOddlySpecific/vosball, `main` in sync
  with `origin/main` (HEAD `1ea7e05`). No backup elsewhere beyond this remote.
- **Fresh-clone note:** the pre-push hook is committed but its activation is
  per-clone — run `git config core.hooksPath .githooks` once on any new clone
  (documented in the hook header). This machine is already configured.
- **Optional follow-ups (not done):**
  - Doc *content* in `docs/` is now path/engine-accurate, but some guides could
    still use a deeper content pass; the financial/draft tools have no UI page yet
    (Finances, Draft Room are planned tiles).
  - `vos_v2` remains archived as a rollback hatch; delete it outright if/when the
    `vosball` engine is fully trusted (golden already proves byte-identical).
  - Consider verifying the app boots in your environment
    (`py -m streamlit run webapp/app.py`) after the `core/` reshuffle — the AppTest
    suite already exercises app import + every page render, but eyes-on is cheap.
- No in-flight batch state.

---

# Session 2 — League Management (ticket 0003)

## Summary
Built core League Management for vosball: a single registry service that
read/writes a league's slice across the ~7 scattered `config/*.json` files, a new
top-level **League Admin** Streamlit page (view / edit / add / remove), and an API
provisioning pipeline that stands up a new league's `teams-` and `park-factors`
files from the `/teams` + `/ballparks` endpoints. Delivered as ticket 0003 Phases
0–4, each phase paused for confirmation. 3 commits on branch
`feat/league-management`.

## Files Changed
**Docs / planning (`b5c12f8`):**
- `tickets/0003-league-management-admin.md` — full plan: per-league settings
  inventory (which keys each tool requires, required/optional/default), the
  `/teams` + `/ballparks` → file field mappings, phased plan, acceptance criteria.
- `tickets/README.md` — index row for 0003.

**Core service layer (`c886da0`):**
- `core/league_registry.py` (new) — `LeagueRegistry` (config-dir injectable).
  `slugs()`/`exists()`/`files_status()`, `load()`→`LeagueConfig`, `save()`
  (merges shared files so `_comment` + sibling leagues + unmodeled keys survive;
  per-league files written when provided), `remove()`, `validate()` (slug/url/
  token-UUID/rating-scale/year/league-ids). Atomic writes (temp→`os.replace`) +
  timestamped backups in `config/.backups/`. Public `write_teams`/
  `write_park_factors` for the large files.
- `core/league_provision.py` (new) — `parse_teams_csv`, `build_park_factors`
  (neutral 1.0 `tool_adjustments`, schema-identical to hand-authored files),
  `build_orgs`, `ml_lid_from_ballparks`, injectable `fetch_text`/`fetch_json`
  (single-shot GETs, browser UA), and `provision_league()` orchestrator
  (writes 3 files + registry entries; refuses to clobber without `overwrite`).
- `tests/test_league_registry.py` (new) — 42 assertions (discovery, load,
  round-trip preservation, add, remove, backups, validation).
- `.gitignore` — ignore `config/.backups/` (token backups are secret).

**UI (`a0ac959`):**
- `webapp/league_admin.py` (new) — League Admin page. Coverage matrix
  (league × every config file), editable connection+metadata form (masked
  token via `apply_edits`), four `st.data_editor` structured editors
  (`parse_league_ids`/`parse_orgs`/`parse_divisions`/`parse_gm_slack`),
  add-league provisioning flow (optional initial `/ratings` fetch) and
  type-to-confirm remove. UI-free parse/save helpers for testability.
- `webapp/app.py` — register the page in `st.navigation`/`_pages`; exempt it
  from the cold-boot gate (it's where you add the first league).
- `webapp/tests/test_webapp.py` — +6 tests (page render, scalar edits,
  structured parsers + round-trip, provision parsers vs the real `/ballparks`
  sample, provision orchestration).

## Deviations from Plan
- **`/ballparks` endpoint introduced mid-plan.** Originally the large
  `teams-`/`park-factors` files were going to be flagged as out-of-scope manual
  imports. The user supplied a new `/ballparks` API + sample, which let Phase 4
  generate them automatically — folded a provisioning pipeline into add-league.
- **Park `tool_adjustments` written neutral (decision (b)).** The scoring engine
  consumes the *derived* `tool_adjustments`, and the raw→adjustments "SAHL
  conversion formulas" exist only as JSON comments (no Python). Rather than gate
  the feature on porting that math, generated files use 1.0 multipliers (real raw
  factors + `team_code` still populated, so `depth_chart`'s name→code map works).
  Porting the formulas is the optional Phase 5.
- **Provision tests live in `webapp/tests/test_webapp.py`** (alongside the admin
  UI tests) rather than `tests/`, even though `league_provision` is a core
  module. Minor; could relocate later.
- **Installed `pytest`** into the user's Python (was absent); the suite is
  pytest-style but also runs standalone via `py webapp/tests/test_webapp.py`.

## Test / Validation Results
- `webapp/tests/test_webapp.py` → **31/31** (was 24 at session start).
- `tests/test_league_registry.py` → **42/42**.
- `tests/test_golden.py` → **4/4** (engine byte-identical — unaffected).
- Core suites (`test_depth_provenance`, `test_fa_fit`, `test_fa_freshness`,
  `test_farm_value_core`, `test_trade_targets_core`) → all pass.
- Registry CLI smoke (`py core/league_registry.py`) reads all 8 real leagues;
  discovery matches `configured_leagues()`. **Real `config/` never mutated** by
  tests (no `config/.backups/` created — confirmed after every phase).
- App boots headless on :8501 (health 200); League Admin reachable.

## Resume Steps
- **Branch `feat/league-management`** has 3 commits (`b5c12f8`, `c886da0`,
  `a0ac959`), not yet pushed and not merged to `main`. Next: push + open a PR (or
  merge) when ready.
- **App is running** in the background (headless, port 8501) — stop it with the
  port-8501 owner kill, or leave it for eyes-on review of League Admin.
- **Optional Phase 5 (not done):** port the SAHL raw→`tool_adjustments`
  conversion formulas into `league_provision` (currently neutral 1.0) and
  generate `park_profile` text from `capacity`/`stadium_type`/`surface` (already
  stashed); add token 90-day expiry reminders in the admin UI.
- **Editing real config writes live files** (atomic + backups in
  `config/.backups/`); `statsplus_tokens.json` and `*-gm-slack.json` stay
  gitignored.
- No in-flight batch state.

---

# Session 3 — League park factors + token age (ticket 0003 Phase 5)

## Summary
Closed out ticket 0003: provisioning now computes real **batting** park
multipliers (Pow/Gap/Eye/Ks) and handedness Power from the raw `/ballparks`
factors via the SAHL formulas — verified to reproduce the hand-authored files
exactly — generates a `park_profile`, and the admin UI tracks token-set dates to
warn near the ~90-day expiry. PR #1 (Phases 0–4) was merged on GitHub
(`3f137bd`) before this work, so Phase 5 sits on a fresh branch off `main`.

## Files Changed
**`62cc256` (branch `feat/league-park-factors`):**
- `core/league_provision.py` — added `compute_batting_adjustments`
  (`Pow=1+(hr-1)·0.6`, `Gap=1+(2B-1)·0.5+(3B-1)·0.3`, `Eye=1+(avg-1)·0.3`,
  `Ks=1+(avg-1)·-0.2`), `compute_handedness_pow`, and `build_park_profile`
  (heuristic type + characteristics + stadium facts). `_neutral_team_entry` →
  `_build_team_entry`: batting/handedness computed, defense/baserunning/pitcher
  left neutral. Raw factors stored at full API precision; only computed
  multipliers rounded (3 dp).
- `webapp/league_admin.py` — `token_age_note` (pure) + a gitignored
  `.ui_settings.json` date store (`_record_token_date`/`_token_set_date`); token
  caption shows 🟢/🟠/🔴 age + days-to-expiry; recorded on edit-save and on
  provisioning when a token is supplied.
- `webapp/tests/test_webapp.py` — updated the provision-parser test for computed
  batting; added `test_park_factor_formulas_match_handauthored` (parity with the
  hand-authored Alabama Bears entry) and `test_token_age_note_levels`.
- `tickets/0003-league-management-admin.md` — Phase 5 marked done + decision note.

## Deviations from Plan
- **Defense/baserunning/pitcher left neutral (user decision).** Inspecting the
  hand-authored files showed those tools have **no conversion formula** (only
  `_rationale_*` keys — hand-tuned per park), unlike the batting tools (clean
  `_calculation_*` formulas). Rather than invent values, only the formula-backed
  batting + handedness tools are computed; the rest stay 1.0.
- **Token age needed new state.** Tokens are bare UUIDs with no creation date, so
  "expires in N days" required recording when a token is set *through the app*
  (gitignored ui-settings). Tokens set outside the app report "age unknown".
- **Raw factors kept unrounded.** First pass rounded raw factors too, which
  clipped `/ballparks`' 4-dp values (e.g. hr 0.9905→0.991); fixed to preserve API
  precision and round only the derived multipliers.

## Test / Validation Results
- `webapp/tests/test_webapp.py` → **33/33** (+2 this session).
- `tests/test_league_registry.py` → **42/42**.
- `tests/test_golden.py` → **4/4** (byte-identical — engine untouched).
- Core suites (depth_provenance, fa_fit, fa_freshness, farm_value_core,
  trade_targets_core) → all pass.
- Sample provision of the Arizona `/ballparks` row → batting
  `{Pow:0.994, Gap:1.246, Eye:1.006, Ks:0.996}`, profile `hitters_park`, handed
  Pow RHB 0.988 / LHB 1.006. Real `config/` never mutated (no `.backups/`).

## Resume Steps
- **Branch `feat/league-park-factors`** = 1 commit (`62cc256`) off `main`,
  pushed → open **PR #2** on GitHub and merge there (same flow as PR #1).
- Phase 5 only affects **newly added** leagues — existing hand-authored
  park-factors files are untouched and stay authoritative.
- App still running headless on :8501 from **pre-Phase-5** code; restart
  (`py -m streamlit run webapp/app.py`) to see the token-age line / new park math.
- Ticket 0003 is fully delivered (Phases 0–5). No further planned phases; no
  in-flight batch state.
